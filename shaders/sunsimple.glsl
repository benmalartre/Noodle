#version 330in vec2 texCoords;out vec4 outColor;uniform float iGlobalTime;uniform vec2 iResolution;uniform vec3 iMouse;uniform mat4 view;#define time iGlobalTimevoid main( ){    float tb = mod(time*4.0  , 32.0);	vec2 uv = texCoords.xy;    uv.x *= iResolution.x/iResolution.y;    vec2 p = 1-uv*2;// - vec2(sin(tb*.01)+.5, tan(tb*.01));	vec3 col = mix(vec3(1,0.5,0),vec3(1.0, 1.0,0),length(p));    float r = 0.5;    col *= smoothstep(r+.1, r+.01, length(p));    outColor = vec4(col, 1.0);